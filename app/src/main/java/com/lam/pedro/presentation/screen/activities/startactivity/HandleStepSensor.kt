package com.lam.pedro.presentation.screen.activities.startactivity

import android.hardware.Sensor
import androidx.compose.material3.SnackbarHostState
import androidx.compose.runtime.Composable
import androidx.compose.runtime.LaunchedEffect

/*
@Composable
fun HandleStepSensor(sensor: Sensor?, snackbarHostState: SnackbarHostState) {
    LaunchedEffect(sensor) {
        if (sensor == null) {
            snackbarHostState.showSnackbar("Step counter sensor is not present on this device")
        } else {
            snackbarHostState.showSnackbar("Step counter sensor OK")
        }
    }
}

 */
