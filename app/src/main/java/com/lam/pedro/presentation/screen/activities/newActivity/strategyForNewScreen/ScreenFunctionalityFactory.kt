package com.lam.pedro.presentation.screen.activities.newActivity.strategyForNewScreen

/*
* Factory for pre-defined screen configurations
* */
/*
object ScreenFunctionalityFactory {
    fun createGpsOnly(): ScreenContext {
        return ScreenContext(listOf(GpsFunctionality()))
    }

    fun createStepsOnly(): ScreenContext {
        return ScreenContext(listOf(StepCounterFunctionality()))
    }

    fun createCombined(): ScreenContext {
        return ScreenContext(listOf(GpsFunctionality(), StepCounterFunctionality()))
    }

    fun createNoFunctionality(): ScreenContext {
        return ScreenContext(listOf(NoFunctionality()))
    }
}

 */
