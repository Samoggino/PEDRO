package com.lam.pedro.data

enum class SessionState {
    IDLE,      // La sessione è inattiva
    RUNNING,   // La sessione è in corso
    PAUSED,    // La sessione è in pausa
    STOPPED    // La sessione è terminata
}
